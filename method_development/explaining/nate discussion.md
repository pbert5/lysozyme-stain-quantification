- add more introductions
    - INTRO should handle framing the problem, , quantifying crypt lyso levels is a popular way to measure inflam from intest insult,  btu do by hand takes time, requires alot of resources, ++ mult undergrads to counter bias who also need training
    - this makes it done in seconds ++ 
    - if have space show it can handle wide range of image/stain quality
    - and say is fast
    - then segues into overview of pipeline, effectivly say as how pipe works, this gets from point A to point B for you without you doing work

need to set everrything of the images in terms of actual poster scale ( check literal poster dpi)
N3 - images need to be bigger: i.e. 
- would be much easier if they were individual modifiable units, have zoombox on image from input data
- can put in caption tooo



- stats:
pipeline: has same quantification, but seperate detection